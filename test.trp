let 
    val greeting = "bonjour"
    val test = if 2 = 3 then 1 else 3 * 4
    fun affine n = 3 + 2*n
in
    let
        val affineTest = affine 5
    in
        print affineTest
    end;
    print test;
    print greeting
end